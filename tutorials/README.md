# Tutorials

[![GitHub](https://img.shields.io/badge/GitHub-181717?style=flat&logo=github&logoColor=white)](https://github.com/kurtvalcorza/tapas-table-question-answering-pipeline)
[![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/kurtvalcorza/tapas-table-question-answering-pipeline/blob/main/tutorials/tapas_table_qa_colab.ipynb)
[![Hugging Face](https://img.shields.io/badge/%F0%9F%A4%97%20Hugging%20Face-google%2Ftapas--large--finetuned--wtq-ffcc4d?style=flat)](https://huggingface.co/google/tapas-large-finetuned-wtq)
[![Upstream](https://img.shields.io/badge/Upstream-google--research%2Ftapas-181717?style=flat&logo=github&logoColor=white)](https://github.com/google-research/tapas)
[![arXiv](https://img.shields.io/badge/arXiv-2004.02349-b31b1b.svg)](https://arxiv.org/abs/2004.02349)

Notebook specification: **DIMER Notebook Specification 2.0** — the notebook is **standalone** (§4) and declares its profile and pedagogical mode (§3.4): it is
generated by `tools/build_notebook.py` from `tools/notebook_template.py` and carries the repository's three
pipeline modules (`pipeline.py`, `metrics.py`, `samples.py`), the model identity, the snapshot manifest and the
runtime pins, so the exported `.ipynb` keeps working without this repository being reachable. Do not edit the
notebook by hand; edit the package or the template and regenerate (`python tools/build_notebook.py`; `--check` is
enforced by the validator and CI).

| Notebook | Profile | Mode | Carrier | Capability | Default runtime | Sample | BYOD | Run-all | Release status |
|---|---|---|---|---|---|---|---|---|---|
| `tapas_table_qa_colab.ipynb` | `E2E` | `GUIDED` | standalone (generated) | table question answering and bounded supervised fine-tuning with `google/tapas-large-finetuned-wtq` (Apache-2.0; TAPAS large, 24 layers, cell-selection + aggregation heads, fine-tuned on SQA → WikiSQL → WTQ) — the safetensors snapshot digest-verified against the inline manifest before loading; the inference contract on the synthetic 4×3 city table (fixed 0.5 sigmoid cell threshold, argmax operator, pipeline-computed numeric answer, input manifest, rejection probe); a digest-pinned real table-question corpus (WikiSQL validation shard, 3.6 MB, SQL programmes executed in pure Python for gold cells and values, MAX/MIN → lookup of the extreme cell) drawn table-disjoint and stratified by operator (240 / 90 / 150); denotation, aggregation and cell accuracy per question type (in the carried `metrics.py`) beside the first-cell and keyword-lookup baselines; bounded fine-tuning of the last two encoder blocks + the three heads with the model's own weak-supervision loss (gold cells label a lookup, an operator question carries only its number) and validation-score epoch selection; held-out evaluation with a four-way comparison and the per-type breakdown; the city table re-answered by the adapted model; safetensors adapter export with verified reload parity | CPU (CUDA used automatically when present); float32 | 240 / 90 / 150 WikiSQL questions (80 lookups + 32 per operator for training; 15 / 25 per operator elsewhere) over disjoint tables, from one pinned Hub parquet shard (3.6 MB) read at run time (BSD-3-Clause) | one JSONL (or JSON list) of `{id, table, question, answer, category?}` records (gated off by default) | ~31 min on the build workstation's CPU after the 1.35 GB checkpoint download (a multiple of that on a 2-vCPU hosted runtime); ~2.5 min on an RTX 5070 Ti | **Candidate** — static checks pass; clean-runtime execution of the `E2E` blob pending in `../docs/release-verification.md` |

## What changed from the inference-only tutorial

The previous notebook (`TASK-INFERENCE`) answered the synthetic city table and reported `sample-sanity` denotation accuracy on three authored questions. This notebook keeps that table and its inference contract as Section 5 and adds the adaptation contract around it: a real labelled table-question corpus fetched with a digest pin and executed for its gold answers, a classification protocol in the carried `metrics.py` (denotation, aggregation and cell accuracy, per category and per operator), two non-neural baselines, a bounded fine-tuning with the checkpoint's own weak-supervision loss, a per-type breakdown, the city table re-answered after adaptation, and an artifact that reloads. The per-grid `evaluation_report` helper is unchanged; `pipe.evaluate` is the corpus-level stage (`measured` / `measured-small-sample`).

## Conformance notes

- **Standalone carrier (ST1–ST8):** the primary path performs no clone, repository install, or repository import. Section 2 embeds the repository's three modules verbatim in topological dependency order (each tagged `metadata.dimer.embedded_module`; the only rewrites make `DEFAULT_WEIGHTS_DIR` working-directory-relative and remove internal relative package imports), Section 3 carries the 6-file snapshot manifest inline and asserts it against the module's constants before fetching, and `NOTEBOOK_SOURCE` records the repository, revision and module digests in the exports.
- **Parity (PAR1–PAR4):** `tests/test_notebook_parity.py` and `tools/validate_release_assets.py` fail when the carried modules, the inline manifest, or the inline pins differ from the repository, or when the notebook is not byte-identical to the generator's output.
- **Model acquisition (MOD1–MOD9, MOD12):** `stage_missing_files(WEIGHTS_DIR, allow_download=True)` fetches only the manifest entries a fresh runtime lacks, at the pinned revision (never `main`); `verify_snapshot` re-hashes every entry and asserts the pinned `model.safetensors` digest and byte count before the tokenizer or the model is constructed; `TAPASTableQAPipeline.from_pretrained(weights_dir=WEIGHTS_DIR)` loads the verified files (`local_files_only=True`, `trust_remote_code=False`) and picks CUDA when it is visible. No cell outside the carried modules calls `huggingface_hub`, `transformers`, `pyarrow`, `safetensors`, `urllib`, `torch.optim` or the model's tokenizer directly.
- **Data (DAT1–DAT9, ST6):** the default sample is drawn from the WikiSQL validation shard converted to parquet by the Hugging Face Hub (`Salesforce/wikisql` at the immutable revision `48cfb60afd0d5f9d2231ca90f76edf9f975181bc`, `default/validation/0000.parquet`, 3,630,670 bytes, SHA-256 `ed524cc7…`): the shard is fetched whole and refused on any byte-count or digest mismatch, its `question` / `table` / `sql` columns decoded, every programme executed in pure Python (`execute_sql`) for the gold cells and value, WikiSQL's `MAX`/`MIN` mapped to a `NONE` lookup of the extreme cell and `AVG` to `AVERAGE`, and the original operator kept as `category`. `wikisql_candidates` keeps 4,972 questions whose tables fit 20 rows / 12 columns / 1,000 characters (nothing is truncated at 512 tokens); `build_sample_dataset(seed=SPLIT_SEED)` assigns tables to splits first and draws 80 lookups + 32 per operator for training and 15 / 25 per operator for validation / test, and `check_split_disjoint` asserts no table (by normalised content) is shared. The template adds a second *External access* bullet naming the repository, the revision, the file, the byte count and the BSD-3-Clause licence.
- **Stages (§4.1):** `fetch_corpus` + `read_corpus` + `wikisql_candidates` + `build_sample_dataset` / `load_byod_dataset` + `split_dataset` (sample acquisition) → `validate_dataset` per split + `check_split_disjoint` + refusal probes (validation, with three digests) → `validate_inputs` + `answer` with sanity checks + `evaluation_report` on the city table (inference contract) → `first_cell_baseline` + `keyword_lookup_baseline` + `pipe.evaluate` on the frozen model with the per-type breakdown (baselines) → `pipe.adapt` (bounded fine-tuning) → `pipe.evaluate` on validation and test with the comparison (evaluation) → `answer` + `evaluation_report` on the city table again (inference) → `pipe.save_artifact` → `TAPASTableQAPipeline.from_artifact` with the parity assertion (export and reload).
- **Score semantics (UNC1–UNC4):** the cell decision is a fixed sigmoid threshold, the operator an argmax, neither a calibrated probability, and the model never abstains; the numeric answer is the pipeline's arithmetic; denotation, aggregation and cell accuracy are own implementations and none is a human judgement; no acceptance threshold ships. The city table re-answered after adaptation carries a `sample-sanity` verdict that names the missing dispersion.
- **Baselines and evidence (EVAL6/EVAL10/EVAL11):** the first-cell floor, the keyword lookup and the frozen model are scored on the same 150 test questions as the adapted model, on the three measures and per question type; every metric is labelled as one seeded split of one sample with no dispersion estimate, and the notebook says explicitly that the frozen model is already a strong lookup model, that the measured gain is the operator choice (aggregation accuracy) with denotation accuracy moving by single questions, and that other training draws in the build record cost lookup accuracy.
- **Split integrity (SPL1–SPL8):** the split is seeded and table-disjoint; validation (90 questions) selects the epoch on the mean of the three measures and the test split is used for nothing but the final evaluation; the notebook states that tables from few documents must be split by source.
- **Ceilings (VAL6):** `MAX_ROWS` (64), `MAX_COLUMNS` (32), `MAX_TOKENS` (512, truncates), `MAX_QUERY_CHARS` (500), `MAX_CELL_CHARS` (200), `AGGREGATIONS`, `CELL_THRESHOLD`, the record bounds (8..20,000) and the id pattern are stated in the Prerequisites and printed before the model runs; four dataset refusals and one input refusal are demonstrated, and a record whose gold cells lie past the truncation point is refused at training time.
- **BYOD (DAT10–DAT19):** `USE_BYOD` defaults to `False` so the sample path never opens an upload dialog; when enabled, one JSONL file (or JSON list) is read into the runtime and its records flow through the same validation, table-disjoint split, baselines, fine-tuning, evaluation, export and reload cells. The expected schema and the privacy guidance are stated in the Prerequisites and in Section 4, and the training split is written to `outputs/tapas_table_qa_train.jsonl` in the same shape.
- Licence: the weights are Apache-2.0 and the notebook, README, card and `docs/WEIGHTS.md` say so; WikiSQL is BSD-3-Clause and is not redistributed — the export records the repository, revision, file, bytes, digest, licence and the sample digest under `corpus`.
- `tools/validate_release_assets.py` performs source validation only. It does not satisfy the clean-runtime execution requirement; a release review must confirm that a recorded clean run in `../docs/release-verification.md` matches the notebook revision under review before the status is promoted to `Release-grade`.

## Outputs

The default notebook path writes (under `outputs/`):

- `tapas_table_qa_train.jsonl` — the training split in the BYOD shape
- `tapas_table_qa_input_manifest.json` — the inference-contract manifest (schema, ceilings, table shape, per-question entries, one recorded rejection finding)
- `tapas_table_qa_evaluation_report.json` — baselines, frozen and adapted metrics on validation and test, the per-type comparison, the adaptation history
- `tapas_table_qa_answers.csv` — the city table's answers before and after adaptation
- `tapas_table_qa_adapter/` — `adapter.safetensors` + `manifest.json`
- `tapas_table_qa_result.json` — `NOTEBOOK_SOURCE`, model identity, snapshot, corpus, inference-contract reports, comparison, artifact digest, reload parity, runtime

The earlier record for the superseded `TASK-INFERENCE` carrier is kept in `../docs/release-verification.md`.

## AI Assistance Disclosure

This repository’s code and accompanying documentation were developed with generative AI assistance for code development and technical writing under maintainer direction. The maintainer remains responsible for reviewing the implementation, validating results, and making release decisions. AI assistance does not constitute independent verification, provider endorsement, or release approval.
