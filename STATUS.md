# Release status

Current status: **Candidate** — tutorial source complete, awaiting clean-runtime execution. The pipeline package with its adaptation contract (`evaluate`, `adapt`, `save_artifact`, `from_artifact`), the digest-pinned WikiSQL sample and the metrics module, the offline unit tests, the model-backed regressions, the MODEL_CARD_SPEC 1.1 `MODEL_CARD.md`, and the standalone `E2E` tutorial notebook `tutorials/tapas_table_qa_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) with its static validator, generator parity checks and CI workflow exist and pass. The tutorial remains non-release-grade until clean-runtime execution evidence for the exact candidate commit is recorded according to `docs/release-verification.md`. The earlier `TASK-INFERENCE` carrier's Kaggle run recorded there is superseded and does not count for the `E2E` blob.
